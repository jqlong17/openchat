# 元规则

- `soul.md`：面向助手的协作约定；`self.md`：面向人类协作者，由人维护。
- `current-state.md`：单页现状；阶段变化时可**整页改写**，避免堆成长日志。
- `trace/`：仅放决策与踪迹；**不**写入密钥或真实凭据。
- 契约与设计在 `workflow/02-design/`；执行计划在 `workflow/03-plan/`。
- **`workflow/03-plan/` 内计划默认按 TDD**：先写失败测试（红）→ 最小实现通过（绿）→ 重构；各阶段 DoD 须含可自动化执行的测试。
